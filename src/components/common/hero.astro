---
import "./hero.css";
// This component receives a slot as the description of the Hero as it could be anything
const { title } = Astro.props;
---

<header class="flex-column-center hero">
  <div class="max-width flex-column-center">
    <h1>{title}</h1>
    <slot />
  </div>
</header>
